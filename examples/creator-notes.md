# Creator notes

This neighboring Markdown file is an optional editable workspace. Canonical machine-readable answers live in `creator-brief.json`. Future approval stages may record SHA-256 source hashes in the brief; this example deliberately reserves an empty map.
